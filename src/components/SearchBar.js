import React from 'react';

const SearchBar = () => {
  return (
    <div className="container">
      <input type="text" className="validate" placeholder="Search robots..." />
    </div>
  );
};

export default SearchBar;
